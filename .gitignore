node_modules/
client/node_modules/
client/dist/
client/dist-demo/
.DS_Store
*.log

# Environment configuration — never commit secrets
.env
.env.*
!.env.example

# The SQLite database is generated on first boot and seeded automatically.
server/*.db
server/*.db.tmp
server/*.db-journal
data/

# Test artefacts
screenshots/

# Generated demo inputs — rebuilt by `npm run demo`, too large to track.
client/src/demo/fixtures.json
